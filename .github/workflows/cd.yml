name: CD

on:
  workflow_run:
    workflows: [ "CI" ]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Portainer
        run: |
          curl -X POST ${{ secrets.ORINOTE_PORTAINER_WEBHOOK }}
